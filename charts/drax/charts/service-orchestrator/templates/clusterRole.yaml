apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "accelleran.service-orchestrator.fullname" . }} 
rules:
- apiGroups:
  - "*"
  resources:
  - "*"
  verbs:
  - "*" 

