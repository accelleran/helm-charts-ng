apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.serviceName }}
  labels:
    {{- include "accelleran.service-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
      protocol: TCP
      targetPort: 8080
      name: {{ .Values.service.name }}
  selector:
    {{- include "accelleran.service-orchestrator.selectorLabels" . | nindent 4 }}
