apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "accelleran.service-monitor.fullname" . }}-test-connection"
  labels:
{{ include "accelleran.service-monitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "accelleran.service-monitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
